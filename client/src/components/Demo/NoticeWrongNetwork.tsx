import React from "react";

const NoticeWrongNetwork = ():React.ReactElement => {
  return (
    <p>MetaMask is not connected to the same network as the one you deployed to.</p>
  )
}

export default NoticeWrongNetwork
